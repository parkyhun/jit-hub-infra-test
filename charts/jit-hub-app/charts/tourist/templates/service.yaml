apiVersion: v1
kind: Service
metadata:
  name: tourist-service
  labels:
    app: tourist                          #  ServiceMonitor 대상 선정용
spec:
  type: ClusterIP
  ports:
  - name: http                            #  ServiceMonitor 포트 지정용
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  selector:
    app: tourist
